

<div>
<% @book.book_comments.each do |book_comment| %>
<%= link_to(book_user) do %>
<%= image_tag book.user.get_profile_image, size:'50x50' %>
<% end %>
<%= book_comment.user.name %>
<%= book_comment.comment %>
<% end %>
</div>

<div>
  <%= form_with model: @book_comment do |f| %>
  <%= f.text_area :comment, rows: '5' , placeholdder: "コメントをここに" %>
  <% end %>
  <%= f.submit "送信"%>
</div>